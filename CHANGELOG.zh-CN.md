# 版本记录

[繁體中文](CHANGELOG.md) · **简体中文** · [English](CHANGELOG.en.md) · [한국어](CHANGELOG.ko.md)

最新的版本在最上面。下载请到 [Releases](https://github.com/MoroseDog/D2RNexus/releases)。

## v0.4.3（2026-09-23）

**修复**

- **帧率上限没有真的生效**：以前 Nexus 把你设的数字写进游戏的“目标帧数”（那是“动态分辨率调节”用的），而把游戏的“帧数上限”固定设成 1。现在直接写进“帧数上限”。游戏的限制会取“帧数上限”和“目标帧数”里较大的那个，所以目标帧数高于上限时会一起降到同一个数字，本来就比较低就不动它。感谢板友 shiun00 反馈。
- 更新后不会自动还原先前被改掉的“目标帧数”。如果你在用“动态分辨率调节”，请到游戏的“显示设置”确认那个值。
- 快捷键字段按下不能用的按键时，下方会直接说明组合键要含 Ctrl、Alt 或 Win；那组快捷键已经被其他账号占用时也会显示。以前完全没有反应，看起来像坏了。

**改进**

- 常见问题新增“用 Token 登录的账号，进游戏只能离线”与“帧率上限设了，实际帧数却不一样”。

## v0.4.2（2026-09-22）

**新增**

- **界面支持多种语言**：繁體中文、简体中文、English、한국어。默认跟随 Windows 的语言，也可以从顶部的“语言”菜单、左下角的地球图标或全局设置切换；切换后会询问是否立即重新打开，已打开的游戏不受影响。

**改进**

- exe 补上了发布者、产品名称、文件说明与版权等文件信息。

## v0.4.1（2026-09-22）

**改进**

- **自动找到游戏**：打开时如果游戏路径不对，会从 Windows 的已安装程序列表找出暗黑2的位置并自动填入。全局设置里也多了“自动查找”按钮。
- **启动失败能看到原因**：账号行会留下红字“启动失败 ⓘ”，鼠标移上去就能看到原因，下次启动时才消失。
- 找不到游戏时，消息会写出是哪个文件、该点击哪个按钮，不再只说“路径”。
- 活动记录有新消息时，会自动滚动到最上面。

**修复**

- 去掉了以前预先填好的示例游戏路径，避免没改到就一直启动失败。
- 去掉了打开时那行容易误解的“启动功能已启用”消息。

## v0.4.0（2026-09-21）

**新增**

- **迷你窗口**：常驻的小列表，总在最前面、不占任务栏，也不会出现在 Alt+Tab 里。每个账号一行，绿点表示游戏开着，点一下切过去；灰点表示没开，点一下就启动它。掉线时补开一个账号，不用再切回主窗口。
- 迷你窗口会显示每个账号的 CPU 与内存占用，CPU 超过 50% 时变红；游戏没有响应时会变成橙点，右键可以直接结束那个游戏。可以收起成一小条，只显示运行中与未启动的数量。
- **每个账号可设一组快捷键**，按下就把那个账号的游戏切到前台。组合键必须包含 Ctrl、Alt 或 Win，避免抢走游戏本身的按键。游戏没开时要不要顺便启动，可以每个账号分别设置。

**改进**

- 启动进行中再点击其他账号的启动或按快捷键，会加入队列接着打开，不再被忽略。
- 活动记录会写出每一批要启动哪些账号。

## v0.3.0（2026-09-20）

**新增**

- **每个账号的画面与性能设置**：窗口分辨率、帧率上限、CPU 优先级，可以沿用全局，也可以每个账号单独设一组。后台账号锁低帧率、把 CPU 优先级设低，让主力账号运行得更流畅。分辨率和帧率都能自定义数值。
- 第一次打开时会读取你当前的游戏设置作为默认值，所以第一次启动不会改动任何东西。之后只有点击“读取当前游戏设置”才会重新读取。
- **右下角图标**：点击关闭时可以选择退出程序或收到任务栏右下角，选择可以记住。收起来之后程序继续运行，点击图标就能调出来。
- **检查新版本**：打开时检查有没有新版并提示，只读取版本号，不会自动下载或安装。可以在全局设置里关闭。
- 再次打开 Nexus 时，会把已经在运行的窗口切到前台，不再弹出警告。

**改进**

- 移除账号与删除分组会先弹出确认窗口，确认按钮倒计时三秒后才能点击，避免误操作。
- 账号设置的布局整理：下拉菜单改为紧跟在标题后面，说明文字更精简。

## v0.2.2（2026-09-20）

**新增**

- 登录窗口多了“用其他方式登录”和“完成登录”两个按钮。用 Apple、Google 这类方式登录、没有 Battle.net 密码的账号，现在也能在 Nexus 里获取 Token，步骤见[用 Apple、Google 等方式登录（繁体中文）](docs/apple-login.md)。这个流程还没有人实际验证过，试过的话欢迎反馈。

## v0.2.1（2026-09-20）

**改进**

- “启动所选”遇到已经开着的账号会跳过，并在活动记录里写一行说明，不再当成错误。多开时掉线了几个，直接再点击一次“启动所选”，就只会补开没开的那几个。
- MOD 名称栏可以直接粘贴“-mod LiYuiMod -txt”这种从其他工具复制来的文字，离开输入框时会自动整理成文件夹名称，并自动开启“启用 MOD”，需要时也会勾选“重新编译 TXT”。
- 找不到 MOD 时，消息会写出完整路径，方便确认 MOD 是不是放在游戏文件夹的 `mods` 里面。
- “额外启动参数”里填了 `-mod`、`-txt` 这些选项时，提示改成了一般人看得懂的说法。
- MOD 名称栏下方加了说明文字。

**修复**

- 用 Apple 等外部方式登录 Battle.net 的账号，之前在登录窗口会被挡住，现在可以在同一个窗口里完成登录。这种情况没有实机验证过，遇到的话欢迎反馈。

**新增**

- [安全说明](SECURITY.zh-CN.md)：需要哪些权限、数据存在哪里、会连接到哪里，以及为什么杀毒软件或 Windows 会弹出警告。
- Release 附上 `SHA256SUMS.txt`，方便核对下载的文件。

## v0.2.0（2026-09-14）

第一个公开测试版。

- 多账号一键依次启动，自动解除多开限制。
- 支持绑定了 OTP 手机验证器的账号：在程序内登录 Battle.net、在手机上批准后保存 Token。
- 分组、自定义启动顺序、列表一键全选或全部取消。
- 每个账号可单独设置登录地区、窗口模式与分辨率、静音、MOD、额外参数，或沿用全局设置。
- 游戏窗口标题改成账号名称，界面上的登录账号自动打码。
- 密码与 Token 用 Windows 内置加密保存在自己的电脑上。
