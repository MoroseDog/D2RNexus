# 版本紀錄

**繁體中文** · [简体中文](CHANGELOG.zh-CN.md) · [English](CHANGELOG.en.md) · [한국어](CHANGELOG.ko.md)

最新的版本在最上面。下載請到 [Releases](https://github.com/MoroseDog/D2RNexus/releases)。

## v0.4.4（2026-09-25）

**新增**

- **迷你視窗可以選擇顯示哪些帳號**：帳號多的時候清單會太長，在那一列按右鍵選「不要顯示在這裡」，或到帳號設定取消勾選「顯示在迷你視窗」。清單下方會寫還有幾個沒顯示、其中幾個正在跑，點旁邊的箭頭展開就能一個一個放回，也可以全部放回。被隱藏的帳號照常啟動，快捷鍵也照常用；收起成小條時的兩個數字仍然算全部帳號。

**改善**

- **各帳號啟動間隔預設改成 5 秒**。以前是 0，前一個遊戲視窗一出現就開下一個，帳號多的時候所有遊戲的載入擠在一起，反而開不起來。已經存過設定的人保留自己的值；勾超過四個帳號而間隔還是 0 時，活動紀錄會提醒一次。
- **等前一個 Token 帳號登入時不再是一片空白**：開始等和等到了各寫一行紀錄（含等了幾秒），帳號列的狀態改成「等前一個帳號按任意鍵登入」，滑鼠移上去可以看到完整的一句話。
- 說明頁新增 Token 帳號的啟動流程圖，以及三則常見問題：用 Token 登入的帳號會停在「按任意鍵即可開始」、亞洲可以玩但歐洲美洲連不上伺服器、多開很卡或一次開太多開不起來。

## v0.4.3（2026-09-23）

**修正**

- **幀數上限沒有真的生效**：以前 Nexus 把你設的數字寫進遊戲的「目標幀數」（那是「動態解析度調節」用的），而把遊戲的「幀數上限」固定設成 1。現在直接寫進「幀數上限」。遊戲的限制會取「幀數上限」和「目標幀數」裡較大的那個，所以目標幀數高於上限時會一起降到同一個數字，本來就比較低就不動它。感謝板友 shiun00 回報。
- 更新後不會自動還原先前被改掉的「目標幀數」。有在用「動態解析度調節」的話，請到遊戲的「顯示設定」確認那個值。
- 快捷鍵欄位按下不能用的按鍵時，下方會直接說明組合鍵要含 Ctrl、Alt 或 Win；那組快捷鍵已經被其他帳號用掉時也會顯示。以前完全沒有反應，看起來像壞掉。

**改善**

- 常見問題新增「用 Token 登入的帳號，進遊戲只能離線」與「幀數上限設了，實際幀數卻不一樣」。

## v0.4.2（2026-09-22）

**新增**

- **介面支援多種語言**：繁體中文、简体中文、English、한국어。預設跟隨 Windows 的語言，也可以從上方「語言」選單、左下角的地球圖示或全域設定切換；換了會問要不要馬上重新開啟，已開啟的遊戲不受影響。

**改善**

- exe 補上發行者、產品名稱、說明與著作權等檔案資訊。

## v0.4.1（2026-09-22）

**改善**

- **自動找到遊戲**：開啟時如果遊戲路徑不對，會從 Windows 的已安裝程式清單找出暗黑 2 的位置自動填入。全域設定也多了「自動尋找」按鈕。
- **啟動失敗看得到原因**：帳號列會留下紅字「啟動失敗 ⓘ」，滑鼠移過去就能看到原因，下次啟動時才消失。
- 找不到遊戲時，訊息會寫出是哪個檔案、該按哪個按鈕，不再只說「路徑」。
- 活動紀錄有新訊息時，會自動捲到最上面。

**修正**

- 拿掉以前預先填好的範例遊戲路徑，避免沒改到就一直啟動失敗。
- 拿掉開啟時那行容易誤會的「啟動功能已啟用」訊息。

## v0.4.0（2026-09-21）

**新增**

- **迷你視窗**：常駐的小清單，總在最前面、不佔工作列、也不會出現在 Alt+Tab。每個帳號一列，綠點代表遊戲開著，點一下切過去；灰點代表沒開，點一下就啟動它。掉線時補開一個帳號不用再切回主視窗。
- 迷你視窗會顯示每個帳號的 CPU 與記憶體用量，CPU 超過五成變紅；遊戲沒有回應時會變成橘點，右鍵可以直接結束那個遊戲。可以收合成一小條，只顯示執行中與未啟動的數量。
- **每個帳號可設一組快捷鍵**，按下就把那個帳號的遊戲叫到前面。組合鍵必須含 Ctrl、Alt 或 Win，避免搶走遊戲本身的按鍵。遊戲沒開時要不要順便啟動，可以每個帳號分別設定。

**改善**

- 啟動進行中再按其他帳號的啟動或快捷鍵，會排進佇列接著開，不再被忽略。
- 活動紀錄會寫出每一批要啟動哪些帳號。

## v0.3.0（2026-09-20）

**新增**

- **每個帳號的畫面與效能設定**：視窗解析度、幀數上限、CPU 優先順序，可以沿用全域，也可以每個帳號各設一組。背景帳號鎖低幀數、把 CPU 優先順序設低，讓主力帳號跑得順一點。解析度和幀數都能自行輸入數字。
- 第一次開啟時會讀取你目前的遊戲設定當預設值，所以第一次啟動不會改到任何東西。之後只有按「讀取目前遊戲設定」才會重讀。
- **右下角圖示**：按關閉鈕時可以選擇結束程式或收到工作列右下角，選擇可以記住。收起來之後程式繼續執行，點圖示就能叫回來。
- **檢查新版本**：開啟時看看有沒有新版並提示，只讀版本號，不會自動下載或安裝。可以在全域設定關閉。
- 再次開啟 Nexus 時，會把已經在執行的視窗叫到前面，不再跳警告。

**改善**

- 移除帳號與刪除群組會先跳確認視窗，確認按鈕倒數三秒才能按，避免手誤。
- 帳號設定的版面整理：下拉選單改成接在標題後面，說明文字精簡。

## v0.2.2（2026-09-20）

**新增**

- 登入視窗多了「用其他方式登入」和「完成登入」兩顆按鈕。用 Apple、Google 這類方式登入、沒有 Battle.net 密碼的帳號，現在也能在 Nexus 裡取得 Token，步驟見[用 Apple、Google 等方式登入](docs/apple-login.md)。這個流程還沒有人實際驗證過，試過歡迎回報。

## v0.2.1（2026-09-20）

**改善**

- 「啟動所選」遇到已經開著的帳號會略過，並在活動紀錄寫一行說明，不再當成錯誤。多開時掉線幾個，直接再按一次「啟動所選」就只補開沒開的那幾個。
- MOD 名稱欄位可以直接貼上「-mod LiYuiMod -txt」這種從其他工具複製來的文字，離開欄位時會自動整理成資料夾名稱，並自動開啟「啟用 MOD」，需要時也會勾起「重新編譯 TXT」。
- 找不到 MOD 時，訊息會寫出完整路徑，方便確認 MOD 是不是放在遊戲資料夾的 `mods` 裡面。
- 「額外啟動參數」填到 `-mod`、`-txt` 這些選項時，訊息改成一般人看得懂的說法。
- MOD 名稱欄位下方加了說明文字。

**修正**

- 用 Apple 等外部方式登入 Battle.net 的帳號，先前在登入視窗會被擋住，現在可以在同一個視窗完成登入。這個情況沒有實機驗證過，有遇到的話歡迎回報。

**新增**

- [安全性說明](SECURITY.md)：需要哪些權限、資料存在哪、會連線到哪裡，以及為什麼防毒軟體或 Windows 會跳出警告。
- Release 附上 `SHA256SUMS.txt`，方便核對下載的檔案。

## v0.2.0（2026-09-14）

第一個公開測試版。

- 多帳號一鍵依序啟動，自動解除多開限制。
- 支援有綁 OTP 手機驗證器的帳號：在程式內登入 Battle.net、手機核准後保存 Token。
- 群組、自訂啟動順序、清單一次全選或全部取消。
- 每個帳號可個別設定登入地區、視窗模式與解析度、靜音、MOD、額外參數，或沿用全域設定。
- 遊戲視窗標題改成帳號名稱、畫面上的登入帳號自動打碼。
- 密碼與 Token 以 Windows 內建加密保存在自己的電腦。
